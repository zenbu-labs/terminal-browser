{
  "name": "release-worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 migrations apply terminal-browser-stats --remote",
    "migrate:local": "wrangler d1 migrations apply terminal-browser-stats --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
