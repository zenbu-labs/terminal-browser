{
  "name": "pixel-react",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build:native && tsc -p tsconfig.json",
    "build:native": "cargo build -p pixel-node && mkdir -p native && rm -f native/pixel.node && cp ../../target/debug/libpixel_node.$(uname | grep -q Darwin && echo dylib || echo so) native/pixel.node"
  },
  "dependencies": {
    "react-reconciler": "^0.29.2"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-reconciler": "^0.28.8",
    "typescript": "^5.5.0"
  }
}
